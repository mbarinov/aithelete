/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['googleusercontent.com', 'picsum.photos','storage.googleapis.com'],
    },
};

export default nextConfig;
